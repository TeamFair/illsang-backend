spring:
  profiles:
    default: local
  application:
    name: module-quest

server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: "*"  # ?? ??? ??: "health", "info", "metrics"
