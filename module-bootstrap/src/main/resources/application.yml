spring:
  application:
    name: module-bootstrap

management:
  endpoints:
    web:
      exposure:
        include: "*"  # "health", "info", "metrics"

logging:
  level:
    root: INFO
    com.teamfair: INFO